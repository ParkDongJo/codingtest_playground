# 수학적 개념

조합 - 순서를 고려하지 않고 뽑는다.
순열 - 순서를 고려하여 뽑는다.

# C++ 에서의 내장함수

next_permutation

- 오름차순으로 배열을 기준으로 모든 순열을 반환함
- 전처리로 정렬을 오름차순으로 미리 해둬야함

prev_permutation

- 오름차순으로 배열을 기준으로 모든 순열을 반환함
- 전처리로 정렬을 내림차순으로 미리 해둬야함

# 만약 문제에서

순서를 재배치하여...
~한 순서의 경우 max 값을...

-> 순열 문제!
