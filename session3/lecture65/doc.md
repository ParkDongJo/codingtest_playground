# DFS vs. BFS

## DFS

- 메모리를 덜 씀.
- 절단점 등 구할 수 있음.
- 코드가 좀 더 짧으며 완전탐색의 경우에 많이 씀.

## BFS

- 메모리를 더 씀
- 가중치가 같은 그래프내에서 최단거리를 구할 수 있음.
- 코드가 더 김


그렇기 때문에, DFS 를 주로 쓴다!
하지만 가중치가 같은 그래프에서 최단거리를 구할때는 BFS 를 쓰는데, 이렇게 쉬운 문제가 나올리가 없다.
가중치가 들어간다면 주로 가중치가 다를 것이고 이때는 다익스트라를 사용해야할지도 모른다